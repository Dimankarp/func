int write(int _);
int read();
void exit(int _);

int return_a(){
    return 97;
}

void main(int a){
    (void-int) ret = return_a;
    // write(ret());

    string sa = "Hello!";
    int ch = sa[3];
    write(ch);


    // int ch = str[4];

    write(10);
    exit(0);
}