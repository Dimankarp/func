int write(int _);
int read();
void exit(int _);

int return_a(){
    return 97;
}

void main(int a){
    (void-int) ret = return_a;
    // write(ret());

    int ch = 68;
    int nch = -67;
    write(-nch);
    write(--ch);
    write(---nch);
    write(----ch);
    write(- - -  - -nch);
    exit(0);
}