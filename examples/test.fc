int write(int _);
int read();
void exit(int _);

int return_a(){
    return 97;
}

void main(int a){
    (void-int) ret = return_a;
    // write(ret());

    bool a = true;
    bool b = false;

    bool c = a == b;
    exit(0);
}