int write(int _);
int read();
void exit(int _);


int cstrlen(string s);
void write_str(string s, int len);
int itoascii(int n);
void write_int(int n);
bool is_digit(int n);
int read_int();

void main(){
    string a = "i love Paris  in the morning";
    string b = "Moscow";
    int i = 7;
    while(i < cstrlen("Moscow") + 7){
        a[i] = b[i-7];
        i = i + 1;
    }
    write_str(a, cstrlen(a));
    write(10);
    exit(0);
}
